// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   // target: "experimental-serverless-trace", // Add this line
//   output: "export",
//   // experimental: {
//   // appDir: true,
//   // },
// };
// module.exports = nextConfig;

// chatGPT;
// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   experimental: {},
// };

// module.exports = nextConfig;

// docs
/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  output: "export",

  // Optional: Change links `/me` -> `/me/` and emit `/me.html` -> `/me/index.html`
  // trailingSlash: true,

  // Optional: Prevent automatic `/me` -> `/me/`, instead preserve `href`
  // skipTrailingSlashRedirect: true,

  // Optional: Change the output directory `out` -> `dist`
  // distDir: 'dist',
};

module.exports = nextConfig;

// /**
//  * @type {import('next').NextConfig}
//  */
// const nextConfig = {
//   output: "export",

//   // Optional: Change links `/me` -> `/me/` and emit `/me.html` -> `/me/index.html`
//   // trailingSlash: true,

//   // Optional: Prevent automatic `/me` -> `/me/`, instead preserve `href`
//   // skipTrailingSlashRedirect: true,

//   // Optional: Change the output directory `out` -> `dist`
//   // distDir: 'dist',
// };

// module.exports = nextConfig;

// ***************************************************************chatgpt
// /**
//  * @type {import('next').NextConfig}
//  */
// const nextConfig = {
//   output: "export",
// };

// module.exports = nextConfig;
// next.config.js
// next.config.js
